# Runtime dependencies for py-msx-emulator
# Python 3.12+ required
#
# Note: pysdl2 requires the SDL2 native library at runtime.
#   macOS:   brew install sdl2
#   Linux:   apt install libsdl2-2.0-0  (or equivalent)
#   Windows: add pysdl2-dll to this list instead

Pillow>=12.0
pysdl2>=0.9.16
PyYAML>=6.0
